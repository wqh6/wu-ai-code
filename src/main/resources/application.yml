spring:
  application:
    name: wu-ai-code
  # mysql
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/yu_ai_code_mother
    username: root
    password: w2004611

  profiles:
    active: local
  data:
    redis:
      host: localhost
      port: 6379
      password:
      ttl: 3600
      database: 0


server:
  port: 8123
  servlet:
    context-path: /api
    session:
        cookie:
          max-age: 1213131232



# springdoc-openapi
springdoc:
  group-configs:
    - group: 'default'
      packages-to-scan: com/example/wuaicode/controller
# knife4j
knife4j:
  enable: true
  setting:
    language: zh_cn

